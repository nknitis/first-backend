# Cypherock MTA Assignment

Client-server implementation for the Cypherock MTA/COT assignment.

## Structure

- `client/` — Node.js + TypeScript client
- `server/` — C++ server
- `proto/` — Protocol Buffers definitions

Implementation will be added incrementally and tested against the assignment requirements.